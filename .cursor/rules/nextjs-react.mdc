---
description: Next.js 16 / React 19 の実装規約
globs: src/**/*.{ts,tsx}
alwaysApply: false
---

# Next.js / React

## 必読

Next.js 16 は学習データと API が異なる。**コード変更前に** `node_modules/next/dist/docs/` の該当ガイドを確認する。

## ページ構成

```tsx
"use client";

import AppShell from "@/components/AppShell";
import { useI18n } from "@/i18n";

export default function SomePage() {
  const { t, ready } = useI18n();
  const copy = t.apps.someApp;

  if (!ready) {
    return (
      <AppShell title={copy.shell.title} description={copy.shell.description}>
        <p className="text-sm text-zinc-400">{t.common.loading}</p>
      </AppShell>
    );
  }

  return (
    <AppShell title={copy.shell.title} description={copy.shell.description}>
      {/* ... */}
    </AppShell>
  );
}
```

## AppShell ルール

分類の正本は `RULEBOOK.md`。

- Type B / C-shell / C-install は `title` と `description` を **必須**
- 1 画面レイアウトは `fillViewport`（SiteChrome の `FILL_VIEWPORT_PATHS` も同時更新）
- C-shell: `isPwa` + `PwaRuntime`（SW なし）+ `STANDALONE_APP_PATHS`
- C-install: ランチ貯金のみ（manifest + SW + 📱）
- Type D は AppShell を使わない
- 言語・バックアップ・インストールの配置 → **`ui-ux.mdc`**

## スタイル

- Tailwind CSS 4。既存の zinc 系パレット・角丸・余白に合わせる
- レイアウト幅は `useLayout()` の `contentClassName` を使う（個別 max-width 乱立を避ける）

## 状態管理

- 永続データ: `useLocalStorageState` または `loadLocalJson` / `saveLocalJson`
- 重いファイル処理: Web Worker や既存ユーティリティのパターンを踏襲
